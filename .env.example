# LangSmith (Optional)
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your_langsmith_api_key
LANGSMITH_PROJECT="email-assistant"

# Gmail API (Optional for Gmail integration)
# Requires credentials.json from Google Cloud Console in the project root